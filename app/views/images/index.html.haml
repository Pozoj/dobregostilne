%h1 Listing images

%table
  %tr
    %th Spot
  - @images.each do |image|
    %tr
      %td= h image.spot_id
        
      %td= link_to 'Show', object_url(image)
      %td= link_to 'Edit', edit_object_url(image)
      %td= link_to 'Destroy', object_url(image), :confirm => 'Are you sure?', :method => :delete

%br/

=link_to 'New image', new_object_url