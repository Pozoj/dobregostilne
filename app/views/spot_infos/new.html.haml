%h1 New spot_info

= error_messages_for :spot_info

- form_for(:spot_info, :url => collection_url) do |f|
  = render :partial => "form", :locals => { :f => f }
  %p= submit_tag "Create"

= link_to 'Back', collection_url